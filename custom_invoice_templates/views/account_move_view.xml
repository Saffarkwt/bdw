<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form_inherit_custom_invoice" model="ir.ui.view">
        <field name="name">account.move.form.inherit.custom.invoice</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header/button[@name='action_post']" position="after">
                <button name="action_print_invoice" string="Print Custom Invoice" type="object" 
                        invisible="move_type not in ['out_invoice', 'in_invoice'] or state != 'posted'"
                        class="oe_highlight"/>
            </xpath>
        </field>
    </record>
</odoo>
