<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Add Print Invoice button to invoice form view -->
    <record id="view_move_form_inherit_custom_invoice" model="ir.ui.view">
        <field name="name">account.move.form.inherit.custom.invoice</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header/button[@name='action_post']" position="after">
                <button name="action_print_invoice" string="Print Custom Invoice" type="object" 
                        attrs="{'invisible': ['|', ('move_type', 'not in', ['out_invoice', 'in_invoice']), ('state', '!=', 'posted')]}"
                        class="oe_highlight"/>
            </xpath>
        </field>
    </record>

    <!-- Override default print button action for invoices -->
    <record id="account.account_invoices" model="ir.actions.report">
        <field name="binding_model_id" eval="False"/>
    </record>
    
    <!-- Add action to print menu -->
    <record id="action_print_custom_invoice" model="ir.actions.server">
        <field name="name">Print Custom Invoice</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">form,list</field>
        <field name="state">code</field>
        <field name="code">
            if records:
                action = records.action_print_invoice()
        </field>
    </record>
</odoo>
